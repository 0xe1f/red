Remote
===
Remote is a Python-based service that is expected to run on the same host as
the emulator. It receives and responds to requests by subscribing to a NATS
topic, and facilitates things like volume control and core launches.
